// User-facing adviser copy, stated once so every surface reads identically.
//
// The boundary line appears on the adviser rail, under the drawer's ask input,
// and in the mobile stacked ask bar. House copy: sentence case, full stop, no
// dashes. The internal "scaffold / never writes applications" vocabulary stays
// in the behaviour contract (contract.ts) and the specs; it is out of
// user-facing copy entirely.

export const ADVISER_BOUNDARY = 'Help with strategy, research and preparation.'
