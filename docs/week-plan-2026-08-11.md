# Week plan — 11 August 2026

Shared context for all working sessions. Read the whole doc; act only on the section you're assigned. Standing rules apply throughout: branches only, propose before implementing anything structural, nothing merges without Paul, sequential work, no subagents unless asked.

## The week in one paragraph

Public launch is targeted for the second week of September. This week has one external clock: the MCP directory submission to Anthropic. If approval lands next week, the public site must be ready to receive that traffic. So the order is: submit the MCP, finish the landing page, make the first-touch surfaces match the new design. The catalogue accuracy work (this week's big push, now largely done) drops to a 30-minute daily review slot. The full design-system rollout across the app is next week's main project, not this week's.

## Section 1 — MCP completion and directory submission (top priority, external clock)

Goal: submit the updated listing form to Anthropic this week.

- The listing goes out under the Shoots identity and shootsfunding.co.uk domain (production already serves this domain). Confirm server URLs, auth endpoints and any hardcoded references match before submission.
- Resolve anything outstanding from the July server audit and the agreed free/paid line before the form goes in.
- The free tier is the marketing taster; the paid line must hold at the entitlement level (apply_access), not just the UI.
- Once submitted, note the expected review timeline so the landing page deadline (section 2) can be set against it.

## Section 2 — Landing page to done (same workstream as section 1)

Goal: live on shootsfunding.co.uk before Anthropic could plausibly approve.

Known remainders:
- Create og-image.png (1200×630).
- Swap the three remaining granttracker.co.uk references in the page file.
- Formal name usage: "Shoots Funding" in cold contexts (title tag, og tags, footer legal line), "Shoots" everywhere else.
- Pricing section stays as "Pricing coming soon" + waitlist. No prices anywhere on the page.
- Final deploy, remove noindex.

## Section 3 — First-touch design pass (this week's only design work)

Goal: every surface a directory visitor can hit on first contact looks like the new design system.

Scope is deliberately small: the /mcp page, auth screens (login, sign-up, forgot/reset password), and anything the directory listing links to directly. Nothing else this week.

- Before starting, re-read the design-migration doc: it records the token decisions and the two never-rename landmines. Inherit those lessons; don't rediscover them.
- The full app-wide rollout is NEXT week's project. Do not expand scope into it.

## Section 4 — Catalogue (30-minute daily slot, no longer the main project)

The verification engine, closures, deadline fixes and split template are done. Remaining items run in the background with short daily reviews:

- Paul activates the 10 staged programme rows via the NEW Review queue → Publish only. Never the old edit-and-approve modal (it pins all 17 fields).
- Julia Rausing: apply is_invite_only = true (directly evidenced by page read, exception to the engine-proposal hold).
- Barrow Cadbury and SSE: fetch per-programme pages, then stage rows with real amounts, not thin shells.
- Somerset's 11 closed funds: capture as between-rounds rows with reopen dates and quotes — future new-fund stock, don't discard.
- Fix the old modal to send only changed fields (pull-forward from the retirement plan; kills the pin factory).
- The 198 fixable links: repair in batches, re-run repaired rows through the engine.
- Daily: check cron_runs each morning (first proof point: LGBT+ fund rolling to 2027-08-12 at 02:00 on 12 Aug).

## Section 5 — Not this week

- Full design-system rollout across the app (next week's main project).
- Old admin surface retirement steps 2–3 (port the nine capabilities, delete the page).
- Category cap / sector-count drift (14 vs 19) investigation.
- Phase 2 feedback (74 twice-rejected grants).
- ADMIN_SECRET rotation + READER_PROXY_URL to Preview (Paul, two minutes in Vercel, any time).

## Paul's personal checklist

- [ ] Send the three cohort emails (Jo, Charlotte, Chris) — drafted, ready
- [ ] Activate the 10 staged rows (new Review queue, Publish)
- [ ] Decide and confirm: MCP listing under Shoots (section 1 assumption)
- [ ] Vercel two-minute visit: fresh ADMIN_SECRET both sides, READER_PROXY_URL to Preview
- [ ] Morning of 12 Aug: check cron_runs for the LGBT+ roll
